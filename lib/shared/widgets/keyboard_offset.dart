import 'package:flutter/material.dart';

class KeyboardOffset extends StatelessWidget {
  const KeyboardOffset({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}